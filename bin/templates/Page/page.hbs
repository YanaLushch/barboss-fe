import { inject, observer } from "mobx-react";
import React, { FC } from "react";
import { RouteComponentProps } from "react-router";

import { Layout } from "./{{pascalCase name}}Page.styles";
import { useScrollToTop } from "../../hooks/useScrollToTop";
import { CommonStore } from "../../core/common/CommonStore";

type InjectedProps = {
  commonStore: CommonStore;
};

type RouteProps = {
  someParam: string;
};

export const {{pascalCase name}}Page: FC<RouteComponentProps<RouteProps> & InjectedProps> = ({ match }) => {
  useScrollToTop();
  return (
    <Layout layoutType="narrow">
      {{pascalCase name}} page
    </Layout>
  );
};

{{pascalCase name}}Page.displayName = "{{pascalCase name}}Page";

export default (inject("commonStore")(observer({{pascalCase name}}Page)) as unknown) as FC<
  RouteComponentProps<RouteProps>
>;
